```json
{
  "slug": "Aqui é uma string única em todo sistema PIN, por meio dessa variável que é estabelecido os noves únicos de tabelas e models no schema Prisma",
  "name": "O nome da aplicação legível para o usuário final",
  "version": "Versão seguindo os padrões de versionamento semântico",
  "description": "Descrição que ficará na store para que o usuário entenda do que se trata a aplicação",
  "io": {
    "input": ["nome em string das funcoes que podem receber dados externos de outros plugins"],
    "output": ["nome das funções que o plugin pode querer usar em determinado uso"]
  }
}
```