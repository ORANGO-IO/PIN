// *** DO NOT EDIT. GENERATED BY PRISMIX. ***
datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Person {
  id    Int      @id @default(autoincrement())
  email String   @unique
  name  String?
  posts Record[] @relation(name: "PersonToRecord")
}

model Record {
  id        Int     @id @default(autoincrement())
  title     String
  content   String?
  published Boolean @default(false)
  author    Person  @relation(name: "PersonToRecord", fields: [authorId], references: [id])
  authorId  Int
}

model Complain {
  id          Int      @id @default(autoincrement())
  name        String
  description String
  Triage      Triage[] @relation(name: "ComplainToTriage")
}

model Triage {
  id                     Int      @id @default(autoincrement())
  complain               Complain @relation(name: "ComplainToTriage", fields: [complainId], references: [id])
  spO2                   Int
  cardiacFreq            Int      @map("cardiac_freq")
  respiratoryFreq        Int      @map("respiratory_freq")
  axilarTemperature      Float    @map("axilar_temperature")
  glucose                Int
  pain                   Int
  systolicBloodPressure  Int      @map("systolic_b_pressure")
  diastolicBloodPressure Int      @map("diastolic_b_pressure")
  complainId             Int      @map("complain_id")
}
